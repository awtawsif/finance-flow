{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account for the FinanceFlow application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Expense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Expense",
      "type": "object",
      "description": "Represents an expense recorded by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Expense entity."
        },
        "description": {
          "type": "string",
          "description": "Description of the expense."
        },
        "amount": {
          "type": "number",
          "description": "Amount of the expense."
        },
        "date": {
          "type": "string",
          "description": "Date of the expense.",
          "format": "date-time"
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to Category."
        }
      },
      "required": [
        "description",
        "amount",
        "date",
        "categoryId"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a category for classifying expenses.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the category (e.g., Food, Transportation)."
        },
        "color": {
          "type": "string",
          "description": "Hex color code for the category."
        }
      },
      "required": [
        "name",
        "color"
      ]
    },
    "Earning": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Earning",
      "type": "object",
      "description": "Represents an earning recorded by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Earning entity."
        },
        "description": {
          "type": "string",
          "description": "Description of the earning."
        },
        "amount": {
          "type": "number",
          "description": "Amount of the earning."
        },
        "date": {
          "type": "string",
          "description": "Date of the earning.",
          "format": "date-time"
        }
      },
      "required": [
        "description",
        "amount",
        "date"
      ]
    },
    "Budget": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Budget",
      "type": "object",
      "description": "Represents a user's budget for a specific category.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Budget entity."
        },
        "limit": {
          "type": "number",
          "description": "Budgeted amount for the category."
        }
      },
      "required": [
        "id",
        "limit"
      ]
    },
    "Shortcut": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Shortcut",
      "type": "object",
      "description": "Represents a user-defined shortcut for quick expense entry.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Shortcut entity."
        },
        "name": {
          "type": "string",
          "description": "User-friendly name for the shortcut (e.g., 'Morning Coffee')."
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "Description of the expense to be created."
        },
        "amount": {
          "type": [
            "number",
            "null"
          ],
          "description": "Amount of the expense."
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to the Category for this expense."
        }
      },
      "required": [
        "id",
        "name",
        "categoryId"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profile information."
    },
    "/users/{userId}/expenses/{expenseId}": {
      "schema": {
        "$ref": "#/entities/Expense"
      },
      "description": "Stores expenses for a user."
    },
    "/users/{userId}/categories/{categoryId}": {
      "schema": {
        "$ref": "#/entities/Category"
      },
      "description": "Stores categories for a user."
    },
    "/users/{userId}/earnings/{earningId}": {
      "schema": {
        "$ref": "#/entities/Earning"
      },
      "description": "Stores earnings for a user."
    },
    "/users/{userId}/budgets/{budgetId}": {
      "schema": {
        "$ref": "#/entities/Budget"
      },
      "description": "Stores budgets for a user, where the document ID is the categoryId."
    },
    "/users/{userId}/shortcuts/{shortcutId}": {
      "schema": {
        "$ref": "#/entities/Shortcut"
      },
      "description": "Stores expense shortcuts for a user."
    }
  }
}
